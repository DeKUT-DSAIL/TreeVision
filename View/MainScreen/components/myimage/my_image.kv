<-MyImage>:
    allow_stretch: True
    keep_ratio: True

    canvas:
        Color:
            rgb: (1, 1, 1, 1)
        RoundedRectangle:
            texture: self.texture
            size: self.size
            pos: self.pos
            radius: self.image_radius